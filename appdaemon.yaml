secrets: /conf/secrets.yaml

appdaemon:
  latitude: !secret home_latitude
  longitude: !secret home_longitude
  elevation: !secret home_elevation
  time_zone: !secret home_timezone
  threads: 20
  plugins:
    MQTT_States:
      type: hassmqtt
      client_host: mqtt
      client_id: testrules
      verbose: true
      publish_topic: events/master
      client_topics:
        - events/slaves/#