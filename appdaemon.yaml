secrets: /conf/secrets.yaml

appdaemon:
  threads: 20
  plugins:
    MQTT_States:
      type: hassmqtt
      client_host: mqtt
      client_id: testrules
      verbose: true
      event_name: MQTT_STATE_EVENT
      client_topics:
        - events/slaves/#